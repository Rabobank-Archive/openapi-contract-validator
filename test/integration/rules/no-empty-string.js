

describe('Rule no-empty-string', function() {
    // TODO: Add tests in 1235671
    it('should be tested');

    // describe('Empty string', function() {
    //     beforeEach(function() {
    //         this.options = {
    //             statusCode: 200,
    //             endpoint: '/endpoint',
    //             method: 'post',
    //             debug: false,
    //         };

    //         this.contract = newContract({
    //             'foo': {
    //                 'type': 'string',
    //             },
    //         });
    //     });

    //     it('throws an error when the string is empty', function(done) {
    //         const response = newResponse({
    //             foo: '',
    //         });

    //         validator.validate(this.contract, response, this.options)
    //             .then(() => {
    //                 throw error;
    //             }, (error_) => {
    //                 expect(c.unstyle(error_.message))
    //                     .to.have.string('NOEMPTY empty strings should be omitted');
    //             })
    //             .then(done, done);
    //     });

    //     it('only contains whitespace', function(done) {
    //         const response = newResponse({
    //             foo: '  \t\n \r ',
    //         });

    //         validator.validate(this.contract, response, this.options)
    //             .then(() => {
    //                 throw error;
    //             }, (error_) => {
    //                 expect(c.unstyle(error_.message))
    //                     .to.have.string('NOEMPTY empty strings should be omitted');
    //             })
    //             .then(done, done);
    //     });
    // });
});
